module:
  language: Python
  processes:
    web:
      command: gunicorn drf-demo.wsgi -b :$PORT --log-file -
#    worker:
#      command: python manage.py celery worker -l info
#    beat:
#      command: python manage.py celery beat -l info